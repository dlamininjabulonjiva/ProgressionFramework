I have created a simple program that calculates the square and cube of a number.

The main purpose of this project is to demonstrate the use of build in delegate (Func<T>).
I have also demonstrated that the Func delegate can be used with an anonymous methods or lambda expression.